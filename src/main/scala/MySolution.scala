object MySolution {


  /*
    case class Item(v: Int, r: Int, c: Int)
  case object ItemOrdering extends Ordering[Item]
  {
    override def CompareTest(x: Item, y: Item): Int = y.v.compareTo(x.v)
  }
  //

    import scala.util.Sorting._
    quickSort[Int](a)(Ordering[Int].reverse)

  */
  def main(args: Array[String]): Unit = {
    val minHeap = collection.mutable.PriorityQueue.empty[Int]

    minHeap.toList



  }
}
